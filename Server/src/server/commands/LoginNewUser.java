package server.commands;

import commands.ObjParamC;
import server.TerminalCommands;

public class LoginNewUser extends ObjParamC<TerminalCommands> {

	public LoginNewUser(TerminalCommands object, String[] strings) {
		super(object, strings);
	}

	@Override
	public void execute(Object... elist) {
		
	}

	
	
}
